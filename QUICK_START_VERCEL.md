# ⚡ Быстрый старт на Vercel

## 🚀 За 5 минут

### 1. Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Импортируйте репозиторий: `https://github.com/ligokit/video-downloader-bot`
4. Добавьте переменные окружения:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
   BOT_USERNAME=ваш_username_бота
   ```
5. Нажмите **"Deploy"**
6. Дождитесь завершения деплоя (1-2 минуты)

### 2. Получите URL

После деплоя вы получите URL вида:
```
https://your-app-name.vercel.app
```

### 3. Установите Webhook

Выполните на вашем компьютере:

```bash
# Клонируйте репозиторий (если еще не сделали)
git clone https://github.com/ligokit/video-downloader-bot.git
cd video-downloader-bot

# Установите зависимости
pip install aiogram python-dotenv

# Создайте .env файл
echo "TELEGRAM_BOT_TOKEN=ваш_токен" > .env

# Установите webhook (замените URL на ваш)
python set_webhook.py https://your-app-name.vercel.app/api/webhook
```

### 4. Готово! 🎉

Откройте вашего бота в Telegram и отправьте `/start`

## 📝 Получение токена бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен
5. **Важно:** Включите inline-режим:
   - Отправьте `/setinline` в BotFather
   - Выберите вашего бота
   - Введите текст подсказки

## ⚠️ Важно знать

- Vercel имеет лимит времени выполнения: **10 секунд** (Hobby) / **60 секунд** (Pro)
- Большие видео могут не успеть загрузиться
- Для production лучше использовать VPS или Railway.app

## 🐛 Проблемы?

### Бот не отвечает
```bash
# Проверьте webhook
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

### Проверьте логи
Vercel Dashboard → Your Project → Logs

## 📚 Подробная документация

См. [VERCEL_DEPLOY.md](VERCEL_DEPLOY.md) для детальных инструкций
