# 🎥 Video Downloader Bot

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/ligokit/video-downloader-bot)

Telegram бот для скачивания видео с YouTube Shorts и TikTok. Поддерживает как прямые сообщения, так и inline-режим для использования в любых чатах.

**GitHub:** [https://github.com/ligokit/video-downloader-bot](https://github.com/ligokit/video-downloader-bot)

## ✨ Возможности

- 📹 Скачивание видео с YouTube Shorts
- 🎵 Скачивание видео с TikTok
- 💬 Работа через прямые сообщения
- 🔄 Inline-режим (@savxbot) для использования в любых чатах
- ⏳ Отображение прогресса загрузки в реальном времени
- 🧹 Автоматическая очистка временных файлов
- ⚡ Асинхронная обработка запросов

## 📋 Требования

- Python 3.10 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- yt-dlp (устанавливается автоматически)

## 🚀 Установка и Деплой

### Вариант 1: Локальный запуск

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd video-downloader-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваши настройки:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
BOT_USERNAME=your_bot_username

# Storage Configuration
TEMP_DIR=./temp
MAX_FILE_SIZE_MB=50

# Cleanup Configuration
CLEANUP_INTERVAL_HOURS=1
MAX_FILE_AGE_HOURS=1
```

### 5. Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл
5. **Важно:** Включите inline-режим:
   - Отправьте `/setinline` в BotFather
   - Выберите вашего бота
   - Введите текст подсказки (например: "Вставьте ссылку на видео")

## 🎮 Запуск

### Локально

```bash
python main.py
```

Бот запустится и начнет обрабатывать сообщения. Для остановки нажмите `Ctrl+C`.

### На Vercel (Serverless)

См. подробную инструкцию в [VERCEL_DEPLOY.md](VERCEL_DEPLOY.md)

**Быстрый старт:**
1. Деплой на Vercel через веб-интерфейс
2. Добавьте переменные окружения
3. Установите webhook: `python set_webhook.py https://your-app.vercel.app/api/webhook`

⚠️ **Важно:** Vercel имеет ограничения по времени выполнения (10-60 сек). Для production рекомендуется VPS или Railway.app.

## 📖 Использование

### Прямые сообщения

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте ссылку на видео:
   - YouTube Shorts: `https://youtube.com/shorts/VIDEO_ID`
   - TikTok: `https://tiktok.com/@user/video/VIDEO_ID`
4. Дождитесь загрузки и получите видео

### Inline-режим

1. В любом чате введите: `@your_bot_username`
2. Вставьте ссылку на видео после имени бота
3. Дождитесь загрузки (статус обновляется автоматически)
4. Выберите готовое видео из результатов для отправки

### Примеры ссылок

**YouTube Shorts:**
```
https://youtube.com/shorts/dQw4w9WgXcQ
https://youtu.be/dQw4w9WgXcQ
```

**TikTok:**
```
https://tiktok.com/@username/video/1234567890123456789
https://vm.tiktok.com/ZMabcdefg/
https://vt.tiktok.com/ZSabcdefg/
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | **Обязательно** |
| `BOT_USERNAME` | Username бота | `savxbot` |
| `TEMP_DIR` | Директория для временных файлов | `./temp` |
| `MAX_FILE_SIZE_MB` | Максимальный размер файла (МБ) | `50` |
| `CLEANUP_INTERVAL_HOURS` | Интервал очистки файлов (часы) | `1` |
| `MAX_FILE_AGE_HOURS` | Максимальный возраст файлов (часы) | `1` |

### Ограничения

- **Максимальный размер видео:** 50 МБ (ограничение Telegram)
- **Поддерживаемые платформы:** YouTube Shorts, TikTok
- **Только публичные видео:** Приватные видео не поддерживаются

## 🏗️ Архитектура

Проект состоит из следующих модулей:

- **main.py** - Точка входа приложения
- **config.py** - Управление конфигурацией
- **url_validator.py** - Валидация и парсинг URL
- **video_downloader.py** - Загрузка видео через yt-dlp
- **storage_manager.py** - Управление временными файлами
- **task_manager.py** - Управление задачами загрузки
- **bot_handler.py** - Обработка Telegram сообщений и inline-запросов
- **background_tasks.py** - Фоновые задачи очистки
- **logger.py** - Настройка логирования

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте правильность токена в `.env`
2. Убедитесь, что бот запущен (`python main.py`)
3. Проверьте логи на наличие ошибок

### Inline-режим не работает

1. Убедитесь, что inline-режим включен в BotFather (`/setinline`)
2. Проверьте, что используете правильный username бота
3. Попробуйте перезапустить бота

### Ошибка "Video too large"

Видео превышает лимит в 50 МБ. Telegram не поддерживает отправку больших файлов через ботов.

### Ошибка "Video unavailable"

Видео может быть:
- Удалено
- Приватное
- Недоступно в вашем регионе
- Защищено от скачивания

## 📝 Логи

Логи выводятся в консоль с уровнем INFO. Формат:

```
2024-01-20 12:00:00 - module_name - INFO - Log message
```

Для изменения уровня логирования отредактируйте `logger.py`.

## 🔒 Безопасность

- Никогда не публикуйте файл `.env` с токеном
- Храните токен бота в безопасности
- Регулярно обновляйте зависимости
- Используйте виртуальное окружение

## 📄 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Изучите логи приложения
3. Создайте issue в репозитории

