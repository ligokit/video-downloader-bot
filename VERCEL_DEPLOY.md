# 🚀 Деплой на Vercel

Это руководство поможет вам задеплоить Video Downloader Bot на Vercel.

## ⚠️ Важные ограничения Vercel

- **Время выполнения:** 10 секунд для Hobby плана, 60 секунд для Pro
- **Размер файлов:** Ограничение на размер загружаемых файлов
- **Хранилище:** `/tmp` очищается после каждого запроса
- **Рекомендация:** Для production лучше использовать VPS или облачные платформы (AWS, Google Cloud, Railway, Render)

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Установленный [Vercel CLI](https://vercel.com/cli) (опционально)
3. Telegram Bot Token от [@BotFather](https://t.me/botfather)

## 🔧 Шаг 1: Подготовка проекта

Проект уже настроен для Vercel с файлом `vercel.json`.

## 🌐 Шаг 2: Деплой на Vercel

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит настройки из `vercel.json`
5. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - ваш токен бота
   - `BOT_USERNAME` - username вашего бота (например: savxbot)
6. Нажмите "Deploy"

### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой проекта
vercel

# Следуйте инструкциям в терминале
```

## 🔗 Шаг 3: Настройка Webhook

После успешного деплоя вы получите URL вида: `https://your-app.vercel.app`

### Установка webhook:

```bash
# Установите webhook (замените URL на ваш)
python set_webhook.py https://your-app.vercel.app/api/webhook
```

Или вручную через curl:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-app.vercel.app/api/webhook"}'
```

### Проверка webhook:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

## ⚙️ Шаг 4: Настройка переменных окружения в Vercel

В настройках проекта на Vercel добавьте:

| Переменная | Значение | Обязательно |
|-----------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Ваш токен от BotFather | ✅ Да |
| `BOT_USERNAME` | Username бота (без @) | ✅ Да |
| `MAX_FILE_SIZE_MB` | 50 | ❌ Нет |
| `CLEANUP_INTERVAL_HOURS` | 1 | ❌ Нет |
| `MAX_FILE_AGE_HOURS` | 1 | ❌ Нет |

## ✅ Шаг 5: Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Отправьте ссылку на YouTube Shorts или TikTok видео
4. Бот должен ответить и начать загрузку

### Проверка webhook endpoint:

```bash
curl https://your-app.vercel.app/api/webhook
```

Должен вернуть:
```json
{
  "status": "ok",
  "message": "Video Downloader Bot webhook is running"
}
```

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что webhook установлен правильно:
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```
3. Проверьте переменные окружения в Vercel

### Ошибка "Function Timeout"

Vercel имеет ограничение времени выполнения:
- Hobby план: 10 секунд
- Pro план: 60 секунд

Для больших видео рекомендуется использовать VPS или другие платформы.

### Inline-режим не работает

1. Убедитесь, что inline-режим включен в BotFather:
   ```
   /setinline
   ```
2. Перезапустите бота (redeploy на Vercel)

## 🔄 Обновление бота

После изменений в коде:

```bash
# Через CLI
vercel --prod

# Или через Git
git push origin master
# (если настроена автоматическая интеграция)
```

## 📊 Мониторинг

- **Логи:** Vercel Dashboard → Your Project → Logs
- **Метрики:** Vercel Dashboard → Your Project → Analytics
- **Webhook статус:** 
  ```bash
  curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
  ```

## 🚨 Важные замечания

1. **Vercel не подходит для long-running процессов** - каждый запрос обрабатывается отдельно
2. **Файлы в /tmp удаляются** после каждого запроса
3. **Ограничение времени выполнения** может прервать загрузку больших видео
4. **Для production рекомендуется:**
   - VPS (DigitalOcean, Linode, Hetzner)
   - Railway.app
   - Render.com
   - AWS Lambda + S3
   - Google Cloud Run

## 🎯 Альтернативы Vercel

Если Vercel не подходит, рассмотрите:

1. **Railway.app** - проще для Python ботов, поддерживает long-running процессы
2. **Render.com** - бесплатный tier, хорошо подходит для ботов
3. **Heroku** - классический вариант (платный)
4. **DigitalOcean App Platform** - простой деплой
5. **VPS** - полный контроль (Hetzner, DigitalOcean Droplets)

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [aiogram Documentation](https://docs.aiogram.dev/)
